# Tools

Local developer tooling and binaries.

- `temporal/` — local Temporal CLI/server helper bits for Windows dev.

If you don't need local binaries checked into your repo, see `docs/checklists/GITHUB_PUSH_CHECKLIST.md`.
